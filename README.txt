To use:
  1) Clone this repository (.dotfiles) in your home folder (~).
  2) Add the following line to .profile or .bashrc:
      for dotfile in ~/.dotfiles/*.sh; do . "$dotfile"; done
  3) Source .profile or .bashrc.
